
general {
	unlock_cmd = notify-send "Welcome back!"
	before_sleep_cmd = notfiy-send "System idle detected!"
	after_sleep_cmd = notfiy-send "Welcome back!"
	ignore_dbus_inhibit = false
}

listener {
	timeout = 60
	on-timeout = hyprlock
}
